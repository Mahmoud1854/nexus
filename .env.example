# Groq API Key
# Sign up at https://console.groq.com to get your API key.
# WARNING: In this client-side app, the API key is exposed to the browser.
# In a production app, route requests through a backend proxy to hide the key.
NEXT_PUBLIC_GROQ_API_KEY=your_groq_api_key_here
